node_modules/
.env
*.log
runs/
.DS_Store

# work-in-progress, not part of the submission
portions/
# chat sessions — never commit conversation history
sessions/

# route memoisation — a local cache, rebuilt on demand
routes/
